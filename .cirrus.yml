container:
  image: "ubuntu:focal"

task:
  tests_script:
    - "apt-get update"
    - "apt-get install -y autoconf automake g++ make"
    - "apt-get install -y parallel libpcre2-dev"
    - "./ci/build.sh"
    - "./check_includes.sh"
