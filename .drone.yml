matrix:
  arch:
    - 64
    - 32
  options:
    - "--enable-debug-logs"
    - ""

build:
  image: "teaci/msys$${arch}"
  pull: true
  shell: "mingw$${arch}"
  commands:
    - "mkdir -p m4"
    - "autoreconf -ifv"
    - "./configure --enable-debug --disable-werror --disable-silent-rules $${options}"
    - "make -j$(nproc)"
    - "./check_includes.sh"
    - "make -j$(nproc) check"

notify:
  irc:
    prefix: " lh_mouse"
    nick: tea-ci
    channel: mcfproj
    server:
      host: chat.freenode.net
      port: 6697
      tls: true
